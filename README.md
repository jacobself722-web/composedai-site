# composedai.app

Marketing landing page for ComposedAI, the speaking coach app.

Single static page (`index.html`), no build step. Served via GitHub Pages at https://composedai.app.

The App Store link lives in the `APP_STORE_URL` constant near the bottom of `index.html`.
